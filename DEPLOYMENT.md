# 投資工具部署方案分析報告

您的專案架構採用 **Streamlit (Python)**，這與傳統的靜態網頁（純 HTML/CSS/JS）不同。傳統的靜態託管平台（如 GitHub Pages）無法直接運行 Python 程式碼。

以下是針對您的需求（個人使用、簡便、免費）對這三個平台以及官方推薦方案的比較：

## 1. 方案比較表

| 平台 | 支援度 | 優點 | 缺點 | 適合對象 |
| :--- | :--- | :--- | :--- | :--- |
| **Streamlit Cloud** | ⭐⭐⭐⭐⭐ | **官方推薦**，完美支援 Python 依賴，一鍵部署。 | 免費版僅支援一個私有倉庫，靜態文件寫入不持久。 | **首選**，特別是快速部署。 |
| **GitHub Pages** | ⭐ | 難度高。需轉換為 WASM (stlite) 才能運行，效能較慢。 | 不支援 Python 後端，無法直接運行。 | 追求極致靜態、不需伺服器的極客。 |
| **Cloudflare Pages** | ⭐⭐ | 速度極快，Edge 網路。 | 同上，需轉換為 WASM。 | 已有 CF 基礎設施使用者。 |
| **Vercel** | ⭐⭐⭐ | 支援 Python Serverless。 | Streamlit 需要長連接 (WebSocket)，Vercel 的冷啟動與連線限制較不適合。 | 熟悉 Vercel 生態系的使用者。 |

---

## 2. 為什麼推薦使用 Streamlit Community Cloud？

對於您的 `InvestmentTool`，我強烈建議使用 **Streamlit Community Cloud**。

### 優點：
1. **原生支援**：不需要修改代碼，直接對接 GitHub。
2. **自動化**：您在 GitHub 推送 code，網站就自動更新。
3. **完全免費**：對於個人使用來說綽綽有餘。

### 🚨 重要注意事項 (資料持久化)
您的專案目前將資料存放在 `watchlist.json`。在雲端平台上：
1. **唯讀性**：雖然可以執行，但在雲端介面上修改資料（例如新增股票）後，**重啟應用或更新代碼時，資料會消失**。
2. **解決方案**：
    * **本地管理**：在電腦上修改好 `watchlist.json` 並 git push。
    * **資料庫**：未來可以考慮接入 Notion API 或 Google Sheets 作為資料庫（Streamlit 支援度非常好）。

---

## 3. 操作教學：如何部署到 Streamlit Cloud

### 第一步：準備 `requirements.txt`
在部署前，您需要讓雲端伺服器知道要安裝哪些套件。

1. 在專案根目錄執行：
   ```bash
   pip freeze > requirements.txt
   ```
   (或者我可以幫您產生一個精簡版)

### 第二步：上傳到 GitHub
1. 在 GitHub 建立一個倉庫（例如 `InvestmentTool`）。
2. 將本地代碼推送到 GitHub：
   ```bash
   git add .
   git commit -m "Deploy to web"
   git push origin main
   ```

### 第三步：連接 Streamlit Cloud
1. 前往 [Streamlit Community Cloud](https://share.streamlit.io/) 並登入 GitHub。
2. 點擊 **"Create app"**。
3. 選擇您的 GitHub 倉庫、分支 (main) 與主文件 (`app.py`)。
4. 點擊 **"Deploy!"**。

---

## 4. 懶人包總結
如果您只想趕快看到網站：
1. **GitHub Pages/Cloudflare/Vercel 都太麻煩**（因為要轉譯 Python）。
2. **請直接用 Streamlit Cloud**。
3. 我下一步會幫您產生 `requirements.txt`，這是部署成功的關鍵。
